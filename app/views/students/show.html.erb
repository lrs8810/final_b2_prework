<h2><%= @student.name %></h2>

<% @student.courses.each do |course| %>
  <section id="course-<%= course.id %>">
    <h4><%= course.name %></h4>
    <p>Grade: <%= course.course_students.first.grade %></p>
  </section>
<% end %>
